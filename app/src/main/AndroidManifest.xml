<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.sweetcompany.sweetie">


    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="com.google.android.providers.gsf.permission.READ_GSERVICES" />
    <uses-permission android:name="tinyTech.us.ua.busschedule.permission.MAPS_RECEIVE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity"
                  android:theme="@style/AppTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".DashboardActivity"
                  android:theme="@style/AppTheme"/>
        <activity android:name=".LoginActivity"
                  android:theme="@style/AppTheme"/>

        <activity android:name=".registration.RegisterActivity" android:theme="@style/AppTheme" />
        <activity android:name=".pairing.PairingActivity" android:theme="@style/AppTheme" />

        <!--android:windowSoftInputMode="adjustNothing"-->
        <activity android:name=".chat.ChatActivity"
            android:theme="@style/AppTheme"
            android:windowSoftInputMode="adjustPan"
            />

        <activity android:name=".gallery.GalleryActivity"
            android:theme="@style/AppTheme"/>
        <activity android:name=".couple.CoupleActivity"
            android:noHistory="true"
            android:theme="@style/AppTheme"/>
        <activity android:name=".couple.CoupleDetailsActivity"
            android:theme="@style/AppTheme"/>
        <activity android:name=".todolist.ToDoListActivity"
            android:theme="@style/AppTheme"/>

        <service
            android:name=".UserMonitorService"
            android:enabled="true"/>

        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

    </application>



</manifest>